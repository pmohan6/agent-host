# Configuration

This directory holds declarative, non-secret configuration for runtimes and
services. Pin exact runtime versions during the runtime phase; do not use floating
`latest` values in recovery-critical configuration.
