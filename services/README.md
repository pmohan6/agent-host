# Services

Each service gets its own directory containing its Compose or native configuration,
health check, state classification, listening-address documentation, and removal
procedure. No service is added merely because its tooling is installed.
