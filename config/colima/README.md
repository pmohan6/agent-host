# Colima

The reviewed Colima profile will be stored here during the container phase. It will
set explicit CPU, memory, disk, VM type, mounts, and autostart behavior rather than
relying on an opaque default VM.
