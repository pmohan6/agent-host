# Script library

Shared shell helpers belong here once two or more scripts need them. Keep helpers
portable to the macOS-provided Bash unless the calling script explicitly requires a
Brewfile-managed shell.
